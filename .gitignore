__pycache__/
*.py[cod]
.env
*.patch.js
server.js
fallback.html

# Real inventory data — never commit (contains real serial numbers / owners)
BD_inventory.csv
*.csv
# Credentials produced by import_csv.py
imported_accounts*.log

# Local throwaway virtualenv for running tests outside Docker
.venv_test/
.pytest_cache/

# TLS certificates: never commit private keys. Regenerate locally via
# nginx/generate_certs.sh (the .gitkeep keeps the dir present after a clone).
nginx/certs/*.pem
